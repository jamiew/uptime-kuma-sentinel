# Uptime Kuma Sentinel Configuration
# Copy this file to .env and edit the values

# Your Uptime Kuma instance URL
KUMA_URL=http://localhost:3001

# Uptime Kuma login credentials
KUMA_USER=admin
KUMA_PASS=changeme

# Monitor configuration
SENTINEL_NAME=INTERNET-SENTINEL
TAG_TO_SUPPRESS=internet-dependent

# Optional: polling interval (currently unused, real-time via WebSocket)
INTERVAL_MS=5000
